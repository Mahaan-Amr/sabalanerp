# تحلیل کامل پیاده‌سازی سنگ اسلب - برچسب‌ها، جریان اندازه‌گیری و فیلترها

## 📋 فهرست مطالب
1. [تحلیل برچسب‌ها و متن‌های UI](#1-تحلیل-برچسب‌ها-و-متن‌های-ui)
2. [تحلیل جریان اندازه‌گیری](#2-تحلیل-جریان-اندازه-گیری)
3. [تحلیل فیلترها و جستجو](#3-تحلیل-فیلترها-و-جستجو)
4. [مقایسه با سنگ طولی و پله](#4-مقایسه-با-سنگ-طولی-و-پله)
5. [الزامات پیاده‌سازی برای سنگ اسلب](#5-الزامات-پیاده-سازی-برای-سنگ-اسلب)
6. [چک‌لیست کامل پیاده‌سازی](#6-چک-لیست-کامل-پیاده-سازی)

---

## 1. تحلیل برچسب‌ها و متن‌های UI

### 1.1 برچسب‌های انتخاب نوع محصول

#### **محل**: Step 4 - انتخاب نوع محصول
```typescript
PRODUCT_TYPES = [
  {
    id: 'longitudinal',
    name: 'سنگ طولی',
    nameEn: 'Longitudinal Stone',
    icon: FaRuler,
    description: 'سنگ‌های طولی با برش طولی',
    available: true
  },
  {
    id: 'stair',
    name: 'سنگ پله',
    nameEn: 'Stair Stone',
    icon: FaSquare,
    description: 'سنگ‌های مخصوص پله',
    available: true
  },
  {
    id: 'slab',
    name: 'سنگ اسلب',
    nameEn: 'Slab Stone',
    icon: FaThLarge,
    description: 'به‌زودی: مدیریت سفارش سنگ‌های اسلب',
    available: false  // ❌ نیاز به تغییر به true
  }
]
```

**نکات مهم**:
- ✅ `slab` تعریف شده است
- ❌ `available: false` - باید به `true` تغییر کند
- ❌ `description` باید به‌روزرسانی شود

### 1.2 برچسب‌های فرم سنگ طولی

#### **بخش 1: اطلاعات پایه**
| برچسب | متن فارسی | واحد/مثال | توضیحات |
|------|---------|----------|---------|
| `نام کامل سنگ` | نام کامل سنگ | - | نمایش خودکار از `generateFullProductName()` |
| `نام یا نوع سنگ` | نام یا نوع سنگ | - | ورودی متنی قابل ویرایش |
| `طول` | طول | cm/m | `FormattedNumberInput` با toggle واحد |
| `عرض` | عرض | cm/m | `FormattedNumberInput` با toggle واحد |
| `تعداد` | تعداد | عدد | `FormattedNumberInput` |
| `متر مربع` | متر مربع | m² | محاسبه خودکار (read-only) |
| `فی هر متر مربع` | فی هر متر مربع (تومان) | تومان | `FormattedNumberInput` |

#### **بخش 2: واحدها**
```typescript
// Toggle buttons برای واحد طول
<button>سانتی‌متر (cm)</button>
<button>متر (m)</button>

// Toggle buttons برای واحد عرض
<button>سانتی‌متر (cm)</button>
<button>متر (m)</button>
```

#### **بخش 3: راهنما**
```typescript
// Help text
"راهنمای انتخاب واحد: برای هر فیلد طول و عرض، روی دکمه‌های 'سانتی‌متر' یا 'متر' کلیک کنید..."
```

#### **بخش 4: Placeholder ها**
| فیلد | Placeholder |
|------|------------|
| طول | `"مقدار طول"` |
| عرض | `"مقدار عرض"` |
| تعداد | `"تعداد"` |
| فی هر متر مربع | `"فی هر متر مربع (تومان)"` |
| متر مربع | - (read-only) |

### 1.3 برچسب‌های فرم سنگ پله (Stair V2)

#### **بخش 1: انتخاب بخش**
```typescript
<label>بخش:</label>
<select>
  <option value="tread">کف پله</option>
  <option value="riser">خیز</option>
  <option value="landing">پاگرد</option>
</select>
```

#### **بخش 2: فیلدهای ورودی**
| برچسب | متن فارسی | واحد | Placeholder | توضیحات |
|------|---------|------|------------|---------|
| `نوع سنگ` | نوع سنگ | - | `"جستجو در نوع برش، جنس سنگ، معدن، نوع پرداخت، رنگ/خصوصیات"` | Search input |
| `قطر` | قطر (سانتی‌متر) | cm | `"ابتدا محصول را انتخاب کنید"` | Disabled, auto-filled |
| `طول` | طول | cm/m | `"مثال: 1.2"` | با toggle واحد |
| `عرض` | عرض (سانتی‌متر) | cm | `"مثال: 40"` | فقط cm |
| `تعداد` | تعداد | عدد | `"مثال: 100"` | |
| `متر مربع` | متر مربع (خودکار) | m² | - | Read-only |
| `قیمت هر متر مربع` | قیمت هر متر مربع | تومان | `"مثال: 1,500,000"` | |

#### **بخش 3: پیام‌های اطلاعاتی**
```typescript
// Info card برای استفاده از سنگ
"از هر سنگ {piecesPerStone} قطعه با عرض {widthCm} سانتی‌متر به دست می‌آید."
"تعداد سنگ پایه مورد نیاز: {baseStoneQuantity} عدد"
"باقیمانده هر سنگ: {leftoverWidthCm}cm"

// Info card برای هزینه برش
"هزینه برش طولی: {cuttingCost} ({lengthM} m × {baseStoneQuantity} سنگ × {cuttingCostPerMeter})"
```

#### **بخش 4: پیام‌های خطا (Validation)**
```typescript
// خطاهای فیلد به فیلد
errors.thickness = `لطفاً ابتدا محصول ${partLabel} را انتخاب کنید`
errors.thickness = `قطر محصول برای ${partLabel} نامعتبر است`
errors.length = `لطفاً طول را برای ${partLabel} وارد کنید`
errors.width = `لطفاً عرض را برای ${partLabel} وارد کنید`
errors.quantity = `لطفاً تعداد را برای ${partLabel} وارد کنید`
errors.pricePerSquareMeter = `لطفاً قیمت هر متر مربع را برای ${partLabel} وارد کنید`
```

### 1.4 برچسب‌های جستجو و فیلتر

#### **جستجوی محصول**
```typescript
placeholder: "جستجو در تمام فیلدها: کد، نام، نوع برش، جنس سنگ، ابعاد، معدن، پرداخت، رنگ، کیفیت، قیمت..."
```

**فیلدهای جستجو شده**:
- `product.code`
- `product.namePersian`
- `product.name`
- `product.cuttingDimensionNamePersian`
- `product.stoneTypeNamePersian`
- `product.widthName`
- `product.thicknessName`
- `product.mineNamePersian`
- `product.finishNamePersian`
- `product.colorNamePersian`
- `product.qualityNamePersian`
- `product.description`
- `product.widthValue` (as string)
- `product.thicknessValue` (as string)
- `product.basePrice` (as string)

#### **جستجوی سنگ در پله**
```typescript
placeholder: "جستجو در نوع برش، جنس سنگ، معدن، نوع پرداخت، رنگ/خصوصیات"
```

#### **جستجوی ابزارها**
```typescript
placeholder: "جستجو در ابزارها"
```

### 1.5 برچسب‌های جدول محصولات

| ستون | برچسب فارسی |
|------|------------|
| `نام کامل محصول` | نام کامل محصول |
| `نام محصول` | نام محصول |
| `ابعاد` | ابعاد (نمایش: `عرض {widthValue} × ضخامت {thicknessValue} سانتی‌متر`) |
| `معدن` | معدن |
| `پرداخت` | پرداخت |
| `قیمت` | قیمت |
| `عملیات` | عملیات |

---

## 2. تحلیل جریان اندازه‌گیری

### 2.1 جریان سنگ طولی (Longitudinal)

#### **مرحله 1: انتخاب محصول**
```
کاربر → جستجو در محصولات → انتخاب محصول → باز شدن مودال پیکربندی
```

#### **مرحله 2: ورود ابعاد**
```
┌─────────────────────────────────────┐
│ 1. انتخاب واحد طول (cm/m)          │
│ 2. وارد کردن طول                   │
│ 3. انتخاب واحد عرض (cm/m)          │
│ 4. وارد کردن عرض                   │
│ 5. وارد کردن تعداد                 │
│ 6. محاسبه خودکار متر مربع         │
│ 7. وارد کردن قیمت هر متر مربع     │
└─────────────────────────────────────┘
```

#### **مرحله 3: محاسبه هوشمند (Smart Calculation)**
```typescript
handleSmartCalculation(changedField, value, currentConfig, lengthUnit, widthUnit, quantity)

// سناریوها:
// 1. تغییر طول → محاسبه متر مربع
// 2. تغییر عرض → محاسبه متر مربع
// 3. تغییر تعداد → محاسبه مجدد متر مربع
// 4. تغییر متر مربع → محاسبه طول یا عرض
```

#### **مرحله 4: بررسی برش خودکار**
```typescript
// اگر عرض کاربر < عرض اصلی → برش خودکار
if (userWidthInCm < originalWidth) {
  isCut = true
  cutType = 'longitudinal'
  // دریافت خودکار قیمت برش
  cuttingCostPerMeter = getCuttingTypePricePerMeter('LONG')
  // محاسبه هزینه برش
  cuttingCost = (length / 100) * cuttingCostPerMeter * quantity
}
```

#### **مرحله 5: اعتبارسنجی**
```typescript
// اعتبارسنجی‌ها:
- عرض نباید بیشتر از عرض اصلی باشد
- طول، عرض، تعداد، قیمت باید وارد شوند
- متر مربع باید > 0 باشد
```

### 2.2 جریان سنگ پله (Stair V2)

#### **مرحله 1: انتخاب بخش**
```
کاربر → انتخاب بخش (کف پله/خیز/پاگرد) → نمایش فرم مربوطه
```

#### **مرحله 2: جستجو و انتخاب سنگ**
```
کاربر → جستجو در سنگ‌ها → انتخاب محصول → 
  → پر شدن خودکار: قطر، قیمت هر متر مربع
  → نمایش نام محصول در جعبه teal
```

#### **مرحله 3: ورود ابعاد**
```
┌─────────────────────────────────────┐
│ 1. طول (با toggle cm/m)            │
│ 2. عرض (فقط cm)                    │
│ 3. تعداد                            │
│ 4. محاسبه خودکار:                  │
│    - متر مربع                      │
│    - قطعات از هر سنگ               │
│    - تعداد سنگ پایه                │
│    - باقیمانده هر سنگ              │
│ 5. قیمت هر متر مربع                │
└─────────────────────────────────────┘
```

#### **مرحله 4: محاسبه استفاده از سنگ**
```typescript
calculateStairStoneUsage(draft) {
  piecesPerStone = floor(originalWidth / userWidth)
  leftoverWidthCm = originalWidth - (piecesPerStone * userWidth)
  baseStoneQuantity = ceil(quantity / piecesPerStone)
  
  return { piecesPerStone, leftoverWidthCm, baseStoneQuantity }
}
```

#### **مرحله 5: محاسبه هزینه برش**
```typescript
// اگر عرض کاربر < عرض اصلی
if (userWidthCm < originalWidthCm) {
  cuttingCostPerMeter = getCuttingTypePricePerMeter('LONG')
  cuttingCost = cuttingCostPerMeter * lengthM * baseStoneQuantity
}
```

#### **مرحله 6: اعتبارسنجی فیلد به فیلد**
```typescript
// هر فیلد خطای خاص خود را دارد:
- نوع سنگ: "لطفاً ابتدا محصول {partLabel} را انتخاب کنید"
- قطر: "قطر محصول برای {partLabel} نامعتبر است"
- طول: "لطفاً طول را برای {partLabel} وارد کنید"
- عرض: "لطفاً عرض را برای {partLabel} وارد کنید"
- تعداد: "لطفاً تعداد را برای {partLabel} وارد کنید"
- قیمت: "لطفاً قیمت هر متر مربع را برای {partLabel} وارد کنید"
```

### 2.3 تفاوت‌های کلیدی جریان

| ویژگی | سنگ طولی | سنگ پله | سنگ اسلب (پیشنهادی) |
|------|---------|---------|---------------------|
| **واحد طول** | cm/m (toggle) | cm/m (toggle) | cm/m (toggle) |
| **واحد عرض** | cm/m (toggle) | cm (ثابت) | cm/m (toggle) |
| **محاسبه خودکار** | متر مربع | متر مربع + استفاده از سنگ | متر مربع + برش دو بعدی |
| **برش خودکار** | برش طولی | برش طولی | برش طولی + عرضی |
| **اعتبارسنجی** | عمومی | فیلد به فیلد | فیلد به فیلد (پیشنهادی) |

---

## 3. تحلیل فیلترها و جستجو

### 3.1 فیلتر محصولات (Product Search)

#### **مکان**: Step 4 - انتخاب محصولات

#### **الگوریتم جستجو**
```typescript
filteredProducts = products.filter(product => {
  // ایجاد رشته جستجو از تمام فیلدها
  const searchableFields = [
    product.code,
    product.namePersian,
    product.name,
    product.cuttingDimensionNamePersian,
    product.stoneTypeNamePersian,
    product.widthName,
    product.thicknessName,
    product.mineNamePersian,
    product.finishNamePersian,
    product.colorNamePersian,
    product.qualityNamePersian,
    product.description,
    product.widthValue?.toString(),
    product.thicknessValue?.toString(),
    product.basePrice?.toString(),
    // ترکیبات ابعاد
    `${product.widthValue}×${product.thicknessValue}`,
    `عرض ${product.widthValue}×ضخامت ${product.thicknessValue}`,
    // نام کامل محصول
    generateFullProductName(product)
  ]
  
  // جستجوی چند کلمه‌ای (AND logic)
  const searchTerms = searchTerm.split(/\s+/)
  return searchTerms.every(term => 
    searchableFields.join(' ').toLowerCase().includes(term)
  )
})
```

#### **ویژگی‌های جستجو**
- ✅ جستجوی چند کلمه‌ای (AND logic)
- ✅ جستجو در تمام فیلدها
- ✅ جستجو در ابعاد (عرض × ضخامت)
- ✅ جستجو در قیمت
- ❌ فیلتر بر اساس نوع محصول (نیاز به افزودن)

### 3.2 فیلتر سنگ در پله (Stone Search for Stair)

#### **مکان**: مودال پیکربندی پله

#### **الگوریتم جستجو**
```typescript
// استفاده از API
const res = await salesAPI.getProducts({ search: term, limit: 10 })

// Deduplication
const uniqueProducts = items.filter((item: any) => {
  if (item.id) {
    if (seenIds.has(item.id)) return false
    seenIds.add(item.id)
    return true
  }
  if (item.code) {
    if (seenCodes.has(item.code)) return false
    seenCodes.add(item.code)
    return true
  }
  return true
})
```

#### **ویژگی‌های جستجو**
- ✅ جستجو از طریق API
- ✅ Deduplication (حذف تکراری‌ها)
- ✅ محدودیت 10 نتیجه
- ❌ فیلتر بر اساس نوع برش (نیاز به بررسی)

### 3.3 فیلتر ابزارها (Tools Search)

#### **مکان**: بخش ابزارها در مودال پله

#### **الگوریتم جستجو**
```typescript
const res = await servicesAPI.getSubServices({ 
  search: term, 
  limit: 20,
  isActive: true 
})
```

---

## 4. مقایسه با سنگ طولی و پله

### 4.1 مقایسه برچسب‌ها

| برچسب | سنگ طولی | سنگ پله | سنگ اسلب (نیاز) |
|------|---------|---------|----------------|
| **طول** | ✅ | ✅ | ✅ (همان) |
| **عرض** | ✅ | ✅ (فقط cm) | ✅ (با toggle cm/m) |
| **ارتفاع** | ❌ | ❌ | ❌ (نیاز نیست - اسلب 2D است) |
| **تعداد** | ✅ | ✅ | ✅ (همان) |
| **متر مربع** | ✅ | ✅ | ✅ (همان) |
| **قیمت هر متر مربع** | ✅ | ✅ | ✅ (همان) |
| **قطر** | ❌ | ✅ (disabled) | ❌ (نیاز نیست) |
| **بخش** | ❌ | ✅ (کف/خیز/پاگرد) | ❌ (نیاز نیست) |

### 4.2 مقایسه جریان اندازه‌گیری

#### **سنگ طولی**
```
1. انتخاب محصول
2. وارد کردن طول (cm/m)
3. وارد کردن عرض (cm/m)
4. وارد کردن تعداد
5. محاسبه خودکار متر مربع
6. بررسی برش خودکار (اگر عرض < عرض اصلی)
7. وارد کردن قیمت
8. افزودن به قرارداد
```

#### **سنگ پله**
```
1. انتخاب بخش (کف/خیز/پاگرد)
2. جستجو و انتخاب سنگ
3. پر شدن خودکار: قطر، قیمت
4. وارد کردن طول (cm/m)
5. وارد کردن عرض (cm)
6. وارد کردن تعداد
7. محاسبه خودکار:
   - متر مربع
   - قطعات از هر سنگ
   - تعداد سنگ پایه
   - باقیمانده
8. بررسی برش خودکار
9. افزودن بخش
```

#### **سنگ اسلب (پیشنهادی)**
```
1. انتخاب محصول
2. وارد کردن طول (cm/m)
3. وارد کردن عرض (cm/m)
4. وارد کردن تعداد
5. محاسبه خودکار متر مربع
6. بررسی برش خودکار (اگر طول < طول اصلی یا عرض < عرض اصلی):
   - برش طولی (اگر عرض < عرض اصلی)
   - برش عرضی (اگر طول < طول اصلی)
7. محاسبه باقیمانده‌ها (چند قطعه 2D)
8. وارد کردن قیمت
9. افزودن به قرارداد
```

### 4.3 مقایسه فیلترها

| ویژگی | سنگ طولی | سنگ پله | سنگ اسلب (نیاز) |
|------|---------|---------|----------------|
| **جستجو در محصولات** | ✅ | ✅ | ✅ (همان) |
| **فیلتر نوع محصول** | ❌ | ❌ | ✅ (افزودن فیلتر برای اسلب) |
| **Deduplication** | ❌ | ✅ | ✅ (همان) |
| **جستجو در ابعاد** | ✅ | ✅ | ✅ (همان) |

---

## 5. الزامات پیاده‌سازی برای سنگ اسلب

### 5.1 تغییرات در برچسب‌ها

#### **الف) به‌روزرسانی PRODUCT_TYPES**
```typescript
{
  id: 'slab',
  name: 'سنگ اسلب',
  nameEn: 'Slab Stone',
  icon: FaThLarge,
  description: 'سنگ‌های اسلب با برش دو بعدی (طول و عرض)', // تغییر
  available: true  // تغییر از false به true
}
```

#### **ب) برچسب‌های جدید برای اسلب**
```typescript
// در فرم پیکربندی اسلب:
{
  // همان برچسب‌های سنگ طولی
  "طول": "طول",
  "عرض": "عرض", 
  "تعداد": "تعداد",
  "متر مربع": "متر مربع (خودکار)",
  "قیمت هر متر مربع": "قیمت هر متر مربع (تومان)",
  
  // برچسب‌های جدید برای برش دو بعدی
  "برش طولی": "برش طولی",
  "برش عرضی": "برش عرضی",
  "هزینه برش": "هزینه برش (طول + عرض)",
  
  // برچسب‌های جدید برای باقیمانده
  "باقیمانده‌ها": "باقیمانده‌های اسلب",
  "نمایش بصری": "نمایش بصری اسلب"
}
```

#### **ج) Placeholder های جدید**
```typescript
// برای طول و عرض (همان سنگ طولی)
placeholder: "مقدار طول"
placeholder: "مقدار عرض"

// برای تعداد (همان)
placeholder: "تعداد"

// برای قیمت (همان)
placeholder: "فی هر متر مربع (تومان)"
```

### 5.2 تغییرات در جریان اندازه‌گیری

#### **الف) افزودن منطق برش دو بعدی**
```typescript
// بررسی برش طولی
const needsLongitudinalCut = userWidthCm < originalWidthCm && userWidthCm > 0

// بررسی برش عرضی
const needsCrossCut = userLengthCm < originalLengthCm && userLengthCm > 0

// محاسبه هزینه برش
if (needsLongitudinalCut) {
  longitudinalCuttingCost = (lengthM * cuttingCostPerMeterLongitudinal * quantity)
}
if (needsCrossCut) {
  crossCuttingCost = (widthM * cuttingCostPerMeterCross * quantity)
}
totalCuttingCost = longitudinalCuttingCost + crossCuttingCost
```

#### **ب) محاسبه باقیمانده‌ها (چند قطعه)**
```typescript
// پس از برش، باقیمانده‌ها می‌توانند چند قطعه باشند:
remainingPieces = [
  { width: remainingWidth, length: usedLength }, // باقیمانده عرضی
  { width: usedWidth, length: remainingLength }, // باقیمانده طولی
  { width: remainingWidth, length: remainingLength } // باقیمانده گوشه
]
```

#### **ج) اعتبارسنجی فیلد به فیلد (مشابه پله)**
```typescript
// برای اسلب نیز باید اعتبارسنجی فیلد به فیلد داشته باشیم:
errors.length = "لطفاً طول را وارد کنید"
errors.width = "لطفاً عرض را وارد کنید"
errors.quantity = "لطفاً تعداد را وارد کنید"
errors.pricePerSquareMeter = "لطفاً قیمت هر متر مربع را وارد کنید"

// اعتبارسنجی ابعاد
if (userLengthCm > originalLengthCm) {
  errors.length = `طول وارد شده بیشتر از طول اصلی اسلب است`
}
if (userWidthCm > originalWidthCm) {
  errors.width = `عرض وارد شده بیشتر از عرض اصلی اسلب است`
}
```

### 5.3 تغییرات در فیلترها

#### **الف) افزودن فیلتر نوع محصول**
```typescript
// در Step 4، افزودن فیلتر برای نوع محصول:
<select value={selectedProductType} onChange={...}>
  <option value="all">همه محصولات</option>
  <option value="longitudinal">سنگ طولی</option>
  <option value="stair">سنگ پله</option>
  <option value="slab">سنگ اسلب</option>
</select>

// فیلتر کردن محصولات بر اساس نوع
const filteredByType = selectedProductType === 'all' 
  ? filteredProducts 
  : filteredProducts.filter(p => 
      p.cuttingDimensionNamePersian === getCuttingDimensionForType(selectedProductType)
    )
```

#### **ب) بهبود جستجو برای اسلب**
```typescript
// جستجو باید شامل محصولات اسلب نیز باشد
// در حال حاضر، جستجو بر اساس cuttingDimensionNamePersian است
// برای اسلب، باید محصولاتی با cuttingDimension = "Tile/Block" یا "Slab" را نشان دهد
```

---

## 6. چک‌لیست کامل پیاده‌سازی

### 6.1 تغییرات در ساختار داده

- [ ] **افزودن `slab` به `productType`**
  ```typescript
  productType: 'longitudinal' | 'stair' | 'slab'
  ```

- [ ] **افزودن فیلدهای اسلب به `ContractProduct`**
  ```typescript
  slab?: {
    originalLength: number;
    originalWidth: number;
    longitudinalCuts: number;
    crossCuts: number;
    remainingPieces: RemainingSlabPiece[];
  }
  ```

- [ ] **به‌روزرسانی `RemainingStone` برای اسلب**
  ```typescript
  // یا ایجاد RemainingSlabPiece جدید
  interface RemainingSlabPiece {
    id: string;
    length: number;
    width: number;
    position: { startLength: number; startWidth: number };
  }
  ```

### 6.2 تغییرات در UI و برچسب‌ها

- [ ] **به‌روزرسانی `PRODUCT_TYPES`**
  - تغییر `available: true` برای `slab`
  - به‌روزرسانی `description`

- [ ] **افزودن برچسب‌های جدید**
  - برچسب "برش طولی"
  - برچسب "برش عرضی"
  - برچسب "باقیمانده‌ها"
  - برچسب "نمایش بصری اسلب"

- [ ] **به‌روزرسانی Placeholder ها**
  - بررسی تمام placeholder ها
  - اطمینان از سازگاری با اسلب

- [ ] **افزودن پیام‌های اطلاعاتی**
  - "برش طولی: {length} m × {price} = {cost}"
  - "برش عرضی: {width} m × {price} = {cost}"
  - "باقیمانده‌ها: {count} قطعه"

### 6.3 تغییرات در جریان اندازه‌گیری

- [ ] **ایجاد `calculateSlabMetrics()`**
  - محاسبه متر مربع
  - محاسبه قیمت
  - محاسبه هزینه برش (طول + عرض)

- [ ] **ایجاد `calculateSlabCutting()`**
  - محاسبه برش طولی
  - محاسبه برش عرضی
  - محاسبه باقیمانده‌ها (چند قطعه)

- [ ] **به‌روزرسانی `handleSmartCalculation()`**
  - پشتیبانی از برش دو بعدی
  - محاسبه باقیمانده‌های چندگانه

- [ ] **افزودن اعتبارسنجی فیلد به فیلد**
  - خطا برای طول
  - خطا برای عرض
  - خطا برای تعداد
  - خطا برای قیمت
  - خطا برای ابعاد (نباید از اصلی بیشتر باشد)

### 6.4 تغییرات در فیلترها

- [ ] **افزودن فیلتر نوع محصول**
  - Dropdown برای انتخاب نوع
  - فیلتر کردن نتایج جستجو

- [ ] **بهبود جستجو**
  - اطمینان از نمایش محصولات اسلب
  - فیلتر بر اساس `cuttingDimension`

### 6.5 تغییرات در نمایش بصری

- [ ] **به‌روزرسانی `StoneCanvas`**
  - پشتیبانی از برش عرضی
  - نمایش چند باقیمانده
  - نمایش موقعیت هر باقیمانده

- [ ] **افزودن نمایش بصری برای اسلب**
  - نمایش اسلب اصلی
  - نمایش ناحیه استفاده شده
  - نمایش باقیمانده‌ها (چند مستطیل)

### 6.6 تغییرات در مودال پیکربندی

- [ ] **ایجاد مودال پیکربندی اسلب**
  - مشابه مودال سنگ طولی
  - با افزودن منطق برش دو بعدی

- [ ] **افزودن بخش باقیمانده‌ها**
  - نمایش لیست باقیمانده‌ها
  - امکان استفاده از باقیمانده‌ها

### 6.7 تست و اعتبارسنجی

- [ ] **تست جریان کامل**
  - انتخاب محصول اسلب
  - وارد کردن ابعاد
  - بررسی برش خودکار
  - بررسی باقیمانده‌ها
  - افزودن به قرارداد

- [ ] **تست اعتبارسنجی**
  - تست خطاهای فیلد به فیلد
  - تست اعتبارسنجی ابعاد
  - تست محاسبات

- [ ] **تست نمایش بصری**
  - تست نمایش برش دو بعدی
  - تست نمایش باقیمانده‌ها
  - تست تعامل با باقیمانده‌ها

---

## 7. خلاصه تفاوت‌های کلیدی

### 7.1 برچسب‌ها

| برچسب | سنگ طولی | سنگ پله | سنگ اسلب |
|------|---------|---------|---------|
| **طول** | ✅ cm/m | ✅ cm/m | ✅ cm/m |
| **عرض** | ✅ cm/m | ✅ cm (ثابت) | ✅ cm/m |
| **قطر** | ❌ | ✅ (disabled) | ❌ |
| **بخش** | ❌ | ✅ | ❌ |
| **برش طولی** | ✅ (خودکار) | ✅ (خودکار) | ✅ (خودکار) |
| **برش عرضی** | ❌ | ❌ | ✅ (نیاز) |
| **باقیمانده** | ✅ (1 قطعه) | ✅ (1 قطعه) | ✅ (چند قطعه) |

### 7.2 جریان اندازه‌گیری

| مرحله | سنگ طولی | سنگ پله | سنگ اسلب |
|------|---------|---------|---------|
| **انتخاب** | محصول | بخش + محصول | محصول |
| **واحد طول** | cm/m | cm/m | cm/m |
| **واحد عرض** | cm/m | cm | cm/m |
| **برش** | 1 جهت | 1 جهت | 2 جهت |
| **باقیمانده** | 1 قطعه | 1 قطعه | چند قطعه |
| **اعتبارسنجی** | عمومی | فیلد به فیلد | فیلد به فیلد |

### 7.3 فیلترها

| ویژگی | سنگ طولی | سنگ پله | سنگ اسلب |
|------|---------|---------|---------|
| **جستجو** | ✅ | ✅ | ✅ |
| **فیلتر نوع** | ❌ | ❌ | ✅ (نیاز) |
| **Deduplication** | ❌ | ✅ | ✅ |

---

## 8. نکات مهم برای پیاده‌سازی

### 8.1 سازگاری با کد موجود

- ✅ استفاده از همان ساختار `ContractProduct`
- ✅ استفاده از همان `FormattedNumberInput`
- ✅ استفاده از همان سیستم اعتبارسنجی
- ✅ استفاده از همان `StoneCanvas` (با به‌روزرسانی)

### 8.2 تفاوت‌های کلیدی

- 🔴 **برش دو بعدی**: نیاز به منطق جدید
- 🔴 **باقیمانده‌های چندگانه**: نیاز به ساختار جدید
- 🔴 **نمایش بصری**: نیاز به به‌روزرسانی `StoneCanvas`
- 🟡 **فیلتر نوع محصول**: نیاز به افزودن dropdown

### 8.3 اولویت‌های پیاده‌سازی

1. **اولویت 1**: به‌روزرسانی `PRODUCT_TYPES` و فعال‌سازی اسلب
2. **اولویت 2**: ایجاد توابع محاسبه برش دو بعدی
3. **اولویت 3**: به‌روزرسانی UI و برچسب‌ها
4. **اولویت 4**: به‌روزرسانی `StoneCanvas` برای نمایش دو بعدی
5. **اولویت 5**: افزودن فیلتر نوع محصول

---

**تاریخ ایجاد**: 2025-01-20  
**نسخه**: 1.0  
**نویسنده**: AI Assistant

