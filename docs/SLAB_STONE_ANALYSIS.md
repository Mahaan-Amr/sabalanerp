# تحلیل جامع سنگ اسلب (Slab Stone) - راهنمای پیاده‌سازی

## 📋 فهرست مطالب
1. [مقدمه و تعریف سنگ اسلب](#1-مقدمه-و-تعریف-سنگ-اسلب)
2. [ابعاد استاندارد سنگ اسلب](#2-ابعاد-استاندارد-سنگ-اسلب)
3. [نحوه فروش سنگ اسلب در صنعت](#3-نحوه-فروش-سنگ-اسلب-در-صنعت)
4. [تفاوت‌های کلیدی با سنگ طولی و پله](#4-تفاوت‌های-کلیدی-با-سنگ-طولی-و-پله)
5. [تحلیل کدبیس فعلی](#5-تحلیل-کدبیس-فعلی)
6. [الزامات پیاده‌سازی](#6-الزامات-پیاده‌سازی)
7. [پیشنهادات طراحی](#7-پیشنهادات-طراحی)

---

## 1. مقدمه و تعریف سنگ اسلب

### 1.1 تعریف
**سنگ اسلب (Slab Stone)** به صفحات بزرگ و یکپارچه سنگ طبیعی گفته می‌شود که معمولاً برای کاربردهای زیر استفاده می‌شوند:
- کانتر آشپزخانه (Kitchen Countertops)
- میزهای ناهارخوری (Dining Tables)
- کف‌پوش‌های لوکس (Luxury Flooring)
- دیوارپوش‌های تزئینی (Decorative Wall Cladding)
- وان و سینک (Bathroom Vanities)

### 1.2 ویژگی‌های کلیدی
- **ابعاد بزرگ**: معمولاً بزرگ‌تر از سنگ‌های طولی هستند
- **یکپارچگی**: هر اسلب یک قطعه منحصر به فرد است
- **الگو و رگه‌های منحصر به فرد**: هر اسلب الگوی رگه‌ای منحصر به فرد دارد
- **قیمت‌گذاری بر اساس متر مربع**: اما با در نظر گیری کیفیت و الگو
- **نیاز به برش دقیق**: برش باید با دقت بالا انجام شود

---

## 2. ابعاد استاندارد سنگ اسلب

### 2.1 ابعاد استاندارد بر اساس نوع سنگ

#### **گرانیت (Granite)**
- **طول**: 108 تا 132 اینچ (9 تا 11 فوت) ≈ 274 تا 335 سانتی‌متر
- **عرض**: 60 تا 78 اینچ (5 تا 6.5 فوت) ≈ 152 تا 198 سانتی‌متر
- **ضخامت**: 2 سانتی‌متر (¾ اینچ) یا 3 سانتی‌متر (1¼ اینچ)

#### **کوارتز (Quartz)**
- **استاندارد**:
  - طول: 120 تا 126 اینچ (10 تا 10.5 فوت) ≈ 305 تا 320 سانتی‌متر
  - عرض: 55 تا 65 اینچ (4.5 تا 5.5 فوت) ≈ 140 تا 165 سانتی‌متر
- **جومبو (Jumbo)**:
  - طول: تا 144 اینچ (12 فوت) ≈ 366 سانتی‌متر
  - عرض: تا 70 اینچ (5.8 فوت) ≈ 178 سانتی‌متر
- **ضخامت**: 2 سانتی‌متر یا 3 سانتی‌متر

#### **مرمر (Marble)**
- **طول**: 100 تا 120 اینچ (8 تا 10 فوت) ≈ 254 تا 305 سانتی‌متر
- **عرض**: 55 تا 75 اینچ (4.5 تا 6.25 فوت) ≈ 140 تا 191 سانتی‌متر
- **ضخامت**: 2 سانتی‌متر یا 3 سانتی‌متر

#### **کوارتزیت (Quartzite)**
- ابعاد مشابه گرانیت
- ضخامت: 2 یا 3 سانتی‌متر

### 2.2 نکات مهم در ابعاد
- ابعاد می‌توانند بر اساس معدن و تولیدکننده متفاوت باشد
- اسلب‌های بزرگ‌تر معمولاً قیمت بالاتری دارند
- اسلب‌های "Jumbo" برای پروژه‌های بزرگ استفاده می‌شوند

---

## 3. نحوه فروش سنگ اسلب در صنعت

### 3.1 روش‌های فروش

#### **الف) فروش بر اساس متر مربع (Square Meter Pricing)**
- قیمت پایه بر اساس متر مربع
- قیمت می‌تواند بر اساس کیفیت، الگو، و نادر بودن متفاوت باشد
- **مثال**: یک اسلب 3 متر × 2 متر = 6 متر مربع × قیمت هر متر مربع

#### **ب) فروش بر اساس قطعه کامل (Whole Slab)**
- بعضی فروشندگان اسلب را به صورت کامل می‌فروشند
- قیمت می‌تواند شامل هزینه برش اولیه باشد
- مناسب برای پروژه‌های بزرگ

#### **ج) فروش بر اساس قطعات برش خورده (Cut Pieces)**
- اسلب برش می‌خورد و قطعات کوچک‌تر فروخته می‌شود
- باقیمانده اسلب می‌تواند برای فروش بعدی نگه داشته شود

### 3.2 عوامل مؤثر بر قیمت

1. **نوع سنگ**: گرانیت، مرمر، کوارتز، کوارتزیت
2. **کیفیت و الگو**: رگه‌های منحصر به فرد، رنگ، بافت
3. **ابعاد**: اسلب‌های بزرگ‌تر معمولاً گران‌تر هستند
4. **ضخامت**: اسلب 3 سانتی‌متر معمولاً گران‌تر از 2 سانتی‌متر است
5. **منبع (معدن)**: معادن خاص قیمت بالاتری دارند
6. **پرداخت (Finish)**: صیقل، مات، براق
7. **نادر بودن**: الگوهای خاص و نادر قیمت بالاتری دارند

### 3.3 فرآیند فروش معمول

1. **انتخاب اسلب**: مشتری اسلب را از انبار انتخاب می‌کند
2. **اندازه‌گیری**: ابعاد مورد نیاز مشتری اندازه‌گیری می‌شود
3. **برش**: اسلب بر اساس نیاز مشتری برش می‌خورد
4. **محاسبه قیمت**: 
   - قیمت بر اساس متر مربع استفاده شده
   - هزینه برش اضافه می‌شود
   - باقیمانده اسلب برای استفاده بعدی نگه داشته می‌شود
5. **پردازش**: پرداخت، لبه‌کاری، و سایر خدمات

### 3.4 مدیریت باقیمانده (Remaining Stone Management)

- **باقیمانده اسلب**: پس از برش، باقیمانده اسلب باید مدیریت شود
- **ذخیره‌سازی**: باقیمانده‌ها باید در انبار ذخیره شوند
- **فروش مجدد**: باقیمانده‌ها می‌توانند برای پروژه‌های دیگر فروخته شوند
- **ردیابی**: باید بتوان باقیمانده‌ها را ردیابی کرد

---

## 4. تفاوت‌های کلیدی با سنگ طولی و پله

### 4.1 مقایسه با سنگ طولی (Longitudinal Stone)

| ویژگی | سنگ طولی | سنگ اسلب |
|------|---------|---------|
| **ابعاد** | طولی (مثلاً 300cm × 60cm) | مستطیلی بزرگ (مثلاً 300cm × 200cm) |
| **برش** | برش طولی (یک جهت) | برش در دو جهت (طول و عرض) |
| **باقیمانده** | یک باقیمانده طولی | چندین باقیمانده در ابعاد مختلف |
| **قیمت‌گذاری** | بر اساس عرض اصلی | بر اساس متر مربع استفاده شده |
| **استفاده** | دیوار، نما | کانتر، میز، کف |

### 4.2 مقایسه با سنگ پله (Stair Stone)

| ویژگی | سنگ پله | سنگ اسلب |
|------|---------|---------|
| **ابعاد** | کوچک و استاندارد | بزرگ و یکپارچه |
| **برش** | چند قطعه از یک سنگ | یک یا چند قطعه از یک اسلب |
| **باقیمانده** | باقیمانده کوچک | باقیمانده بزرگ قابل استفاده |
| **قیمت‌گذاری** | بر اساس تعداد و ابعاد | بر اساس متر مربع |
| **استفاده** | پله | کانتر، میز |

### 4.3 چالش‌های خاص سنگ اسلب

1. **برش دو بعدی**: باید بتوان در هر دو جهت (طول و عرض) برش داد
2. **مدیریت باقیمانده پیچیده**: باقیمانده‌ها می‌توانند در ابعاد مختلف باشند
3. **ردیابی الگو**: هر اسلب الگوی منحصر به فرد دارد
4. **ذخیره‌سازی**: نیاز به فضای بیشتر برای ذخیره‌سازی
5. **نمایش بصری**: نیاز به نمایش بصری پیچیده‌تر برای نشان دادن برش‌ها

---

## 5. تحلیل کدبیس فعلی

### 5.1 ساختار فعلی محصولات

#### **نوع محصول (Product Type)**
```typescript
productType: 'longitudinal' | 'stair' | 'slab' | 'volumetric'
```

در حال حاضر:
- ✅ `longitudinal`: پیاده‌سازی شده
- ✅ `stair`: پیاده‌سازی شده
- ❌ `slab`: تعریف شده اما `available: false`
- ❌ `volumetric`: تعریف شده اما `available: false`

#### **ساختار ContractProduct**
```typescript
interface ContractProduct {
  productId: string;
  product: Product;
  productType: 'longitudinal' | 'stair';
  
  // ابعاد
  length: number;
  width: number;
  quantity: number;
  squareMeters: number;
  
  // قیمت‌گذاری
  pricePerSquareMeter: number;
  totalPrice: number;
  
  // برش
  isCut: boolean;
  cutType: 'longitudinal' | 'cross' | null;
  originalWidth: number;
  originalLength: number;
  cuttingCost: number;
  cuttingCostPerMeter: number;
  
  // باقیمانده
  remainingStones: RemainingStone[];
  cutDetails: StoneCut[];
}
```

### 5.2 منطق برش فعلی

#### **سنگ طولی (Longitudinal)**
- برش در یک جهت (طول)
- باقیمانده: یک قطعه با عرض باقیمانده
- تابع: `calculateStoneCutting()`
- نمایش بصری: `StoneCanvas` با برش طولی

#### **سنگ پله (Stair)**
- برش در یک جهت (طول)
- چند قطعه از یک سنگ پایه
- تابع: `calculateStairStoneUsage()`
- نمایش بصری: `StoneCanvas` با چند قطعه

### 5.3 توابع کلیدی موجود

#### **1. calculateStoneMetrics()**
```typescript
// محاسبه متر مربع، قیمت، هزینه برش
calculateStoneMetrics({
  length, width, quantity,
  squareMeters, pricePerSquareMeter,
  isCut, originalWidth, cuttingCostPerMeter
})
```

#### **2. calculateStoneCutting()**
```typescript
// محاسبه برش طولی
calculateStoneCutting({
  originalWidth, desiredWidth, length,
  cuttingCostPerMeter, quantity
})
```

#### **3. calculateStairStoneUsage()**
```typescript
// محاسبه استفاده از سنگ برای پله
calculateStairStoneUsage(draft: StairPartDraftV2)
// Returns: piecesPerStone, baseStoneQuantity, leftoverWidthCm
```

### 5.4 نمایش بصری (StoneCanvas)

- ✅ پشتیبانی از برش طولی
- ✅ نمایش باقیمانده‌ها
- ✅ نمایش ابزارها (SubServices)
- ❌ پشتیبانی از برش عرضی (Cross Cut)
- ❌ پشتیبانی از برش دو بعدی (طول و عرض)

### 5.5 مدیریت باقیمانده

#### **RemainingStone Interface**
```typescript
interface RemainingStone {
  id: string;
  width: number; // عرض باقیمانده (cm)
  length: number; // طول باقیمانده (meters)
  squareMeters: number;
  isAvailable: boolean;
  sourceCutId: string;
  position?: {
    startWidth: number;
    startLength: number;
  };
}
```

**نکته**: این ساختار برای برش یک بعدی طراحی شده است.

---

## 6. الزامات پیاده‌سازی

### 6.1 الزامات عملکردی (Functional Requirements)

#### **FR1: انتخاب اسلب**
- کاربر باید بتواند اسلب را از لیست محصولات انتخاب کند
- نمایش ابعاد اصلی اسلب (طول × عرض × ضخامت)
- نمایش الگو و کیفیت اسلب

#### **FR2: تعیین ابعاد مورد نیاز**
- کاربر باید بتواند ابعاد مورد نیاز را وارد کند (طول × عرض)
- پشتیبانی از واحدهای cm و m
- اعتبارسنجی: ابعاد نباید از ابعاد اصلی اسلب بیشتر باشد

#### **FR3: برش دو بعدی**
- برش در جهت طول (Longitudinal Cut)
- برش در جهت عرض (Cross Cut)
- محاسبه هزینه برش برای هر دو جهت

#### **FR4: محاسبه قیمت**
- قیمت بر اساس متر مربع استفاده شده
- هزینه برش (طول + عرض)
- قیمت کل = (متر مربع × قیمت هر متر مربع) + هزینه برش

#### **FR5: مدیریت باقیمانده**
- محاسبه باقیمانده‌ها پس از برش
- ذخیره باقیمانده‌ها برای استفاده بعدی
- نمایش باقیمانده‌ها در نمایش بصری

#### **FR6: نمایش بصری**
- نمایش اسلب اصلی
- نمایش ناحیه استفاده شده
- نمایش باقیمانده‌ها (چند قطعه)
- نمایش برش‌ها (طول و عرض)

### 6.2 الزامات غیرعملکردی (Non-Functional Requirements)

#### **NFR1: کارایی**
- محاسبات باید سریع انجام شوند
- نمایش بصری باید روان باشد

#### **NFR2: قابلیت استفاده**
- رابط کاربری باید ساده و قابل فهم باشد
- راهنمای استفاده باید واضح باشد

#### **NFR3: سازگاری**
- باید با سیستم فعلی سازگار باشد
- باید از همان ساختار داده استفاده کند

---

## 7. پیشنهادات طراحی

### 7.1 ساختار داده پیشنهادی

#### **افزودن به ContractProduct**
```typescript
interface ContractProduct {
  // ... فیلدهای موجود
  
  // برای اسلب
  slab?: {
    originalLength: number; // طول اصلی اسلب (cm)
    originalWidth: number; // عرض اصلی اسلب (cm)
    originalThickness: number; // ضخامت اصلی اسلب (cm)
    
    // برش‌ها
    longitudinalCuts: number; // تعداد برش‌های طولی
    crossCuts: number; // تعداد برش‌های عرضی
    
    // باقیمانده‌ها (چند قطعه)
    remainingPieces: RemainingSlabPiece[];
  };
}

interface RemainingSlabPiece {
  id: string;
  length: number; // طول باقیمانده (cm)
  width: number; // عرض باقیمانده (cm)
  thickness: number; // ضخامت (cm)
  squareMeters: number;
  position: {
    startLength: number; // موقعیت شروع در طول (cm)
    startWidth: number; // موقعیت شروع در عرض (cm)
  };
  isAvailable: boolean;
}
```

### 7.2 توابع پیشنهادی

#### **1. calculateSlabCutting()**
```typescript
function calculateSlabCutting(data: {
  originalLength: number; // طول اصلی اسلب (cm)
  originalWidth: number; // عرض اصلی اسلب (cm)
  desiredLength: number; // طول مورد نیاز (cm)
  desiredWidth: number; // عرض مورد نیاز (cm)
  cuttingCostPerMeterLongitudinal: number; // هزینه برش طولی
  cuttingCostPerMeterCross: number; // هزینه برش عرضی
}) {
  // محاسبه برش‌ها
  // محاسبه باقیمانده‌ها
  // محاسبه هزینه برش
  return {
    cutDetails: [...],
    remainingPieces: [...],
    totalCuttingCost: number
  };
}
```

#### **2. calculateSlabMetrics()**
```typescript
function calculateSlabMetrics(data: {
  length: number;
  width: number;
  quantity: number;
  pricePerSquareMeter: number;
  // ... سایر پارامترها
}) {
  // محاسبه متر مربع
  // محاسبه قیمت
  return {
    squareMeters: number,
    totalPrice: number,
    // ...
  };
}
```

### 7.3 رابط کاربری پیشنهادی

#### **مرحله 1: انتخاب اسلب**
- نمایش لیست اسلب‌های موجود
- فیلتر بر اساس نوع، ابعاد، قیمت
- نمایش تصویر و مشخصات

#### **مرحله 2: تعیین ابعاد**
```
┌─────────────────────────────────┐
│ ابعاد اسلب اصلی                 │
│ طول: 300 cm  عرض: 200 cm       │
├─────────────────────────────────┤
│ ابعاد مورد نیاز                 │
│ طول: [____] cm/m                │
│ عرض: [____] cm/m                │
│ تعداد: [____]                    │
├─────────────────────────────────┤
│ محاسبه خودکار                   │
│ متر مربع: 6.00 m²              │
│ قیمت: 9,000,000 تومان           │
└─────────────────────────────────┘
```

#### **مرحله 3: نمایش بصری**
- نمایش اسلب اصلی
- نمایش ناحیه استفاده شده
- نمایش باقیمانده‌ها (چند قطعه)

### 7.4 منطق برش دو بعدی

#### **سناریو 1: یک قطعه از اسلب**
```
اسلب اصلی: 300cm × 200cm
قطعه مورد نیاز: 150cm × 100cm

برش‌ها:
- 1 برش طولی (300cm → 150cm + 150cm)
- 1 برش عرضی (200cm → 100cm + 100cm)

باقیمانده‌ها:
- قطعه 1: 150cm × 100cm (موقعیت: 0, 0)
- قطعه 2: 150cm × 100cm (موقعیت: 150, 0)
- قطعه 3: 150cm × 100cm (موقعیت: 0, 100)
```

#### **سناریو 2: چند قطعه از اسلب**
```
اسلب اصلی: 300cm × 200cm
قطعات مورد نیاز:
- 2 قطعه 100cm × 80cm

برش‌ها:
- 2 برش طولی
- 2 برش عرضی

باقیمانده‌ها:
- چند قطعه در ابعاد مختلف
```

### 7.5 به‌روزرسانی StoneCanvas

#### **پشتیبانی از برش دو بعدی**
- نمایش برش‌های طولی و عرضی
- نمایش چند باقیمانده
- نمایش موقعیت هر باقیمانده

#### **الگوریتم نمایش**
1. رسم اسلب اصلی
2. رسم ناحیه استفاده شده
3. رسم باقیمانده‌ها (چند مستطیل)
4. نمایش برش‌ها (خطوط)

---

## 8. خلاصه و توصیه‌ها

### 8.1 خلاصه تفاوت‌ها

| ویژگی | سنگ طولی | سنگ پله | سنگ اسلب |
|------|---------|---------|---------|
| **ابعاد** | طولی | کوچک | بزرگ مستطیلی |
| **برش** | یک جهت | یک جهت | دو جهت |
| **باقیمانده** | یک قطعه | یک قطعه | چند قطعه |
| **قیمت‌گذاری** | عرض اصلی | عرض اصلی | متر مربع استفاده شده |
| **پیچیدگی** | متوسط | متوسط | بالا |

### 8.2 توصیه‌های پیاده‌سازی

1. **گام 1**: افزودن نوع محصول `slab` به `productType`
2. **گام 2**: ایجاد توابع محاسبه برش دو بعدی
3. **گام 3**: به‌روزرسانی `StoneCanvas` برای نمایش دو بعدی
4. **گام 4**: ایجاد رابط کاربری برای انتخاب و تعیین ابعاد
5. **گام 5**: تست با سناریوهای مختلف

### 8.3 نکات مهم

- سنگ اسلب پیچیده‌تر از سنگ طولی و پله است
- نیاز به مدیریت باقیمانده‌های چندگانه دارد
- نمایش بصری باید قابلیت نمایش برش دو بعدی را داشته باشد
- قیمت‌گذاری بر اساس متر مربع استفاده شده است (نه عرض اصلی)

---

## 9. منابع و مراجع

- [Stone Park USA - Slab Dimensions](https://stoneparkusa.com/blog/how-big-is-a-stone-slab/)
- [Online Slab Store](https://www.onlineslabstore.com/)
- [Stone Warehouse Online](https://stonewarehouseonline.com/slab-warehouse/)

---

**تاریخ ایجاد**: 2025-01-20  
**نسخه**: 1.0  
**نویسنده**: AI Assistant

