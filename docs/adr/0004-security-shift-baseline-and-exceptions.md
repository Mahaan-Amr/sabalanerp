# Preserve the annual security rota as a baseline with explicit exceptions

Security shift planning uses a published, generated A→B→C baseline with exactly three primary guards and configurable slot duration. Leave, substitutions, temporary coverage, emergency gaps, attendance corrections, and mid-year personnel changes are recorded as explicit exceptions or future plan revisions rather than rewriting started or historical slots; this keeps each guard's expected rotation stable and makes actual coverage auditable.
