# Version collateral requirements as ordered bundles

A management collateral requirement is one versioned, ordered bundle of stable requirement lines, and Finance creates a separate fulfillment item for every accepted line. We chose bundle versions over editing independent rows so the applicant accepts one coherent offer condition, post-registration changes cannot rewrite prior consent, and confidential custody details remain outside the pre-acceptance requirement model.
