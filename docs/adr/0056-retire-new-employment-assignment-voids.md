# Retire new Employment Assignment voids

Human Resources uses one ordinary correction per assignment state: cancel a future assignment or end a current assignment with an effective date and reason, including a current assignment created for testing. New void operations are retired from every role and API because offering both void and end creates an unsafe choice that can erase a real current placement; existing void records remain immutable, readable audit evidence and are never rewritten as ended assignments.
