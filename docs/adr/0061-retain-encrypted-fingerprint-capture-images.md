# Retain encrypted fingerprint capture images with Personnel enrollment

SabalanERP permanently retains each enrolled finger's PNG capture image, encrypted with context-bound biometric keys and attached to the Personnel-owned biometric enrollment. We chose persistent, audited HR viewing over ephemeral preview or plaintext files so operators can inspect the enrolled capture later without placing image bytes in logs, JSON evidence, ordinary dispatch exports, or login User records.
